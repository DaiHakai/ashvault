# Production: obtain this from your managed PostgreSQL provider.
DATABASE_URL=postgresql://user:password@host:5432/ashvault

# Use a long, random value. Never commit the real value.
AUTH_SECRET=replace-with-a-long-random-secret
NODE_ENV=production
